using UnityEngine;

namespace Figures.Animals
{
    public class FigureAnimal : Figure
    {
        public FigureAnimal(Color figureColor, bool isTarget) : base(figureColor, isTarget)
        {
        }
    }
}